# ──────────────────────────────────────────────────────────────────────
# IMFSL-HR Environment Configuration
# Copy this file to .env and fill in the values.
# NEVER commit .env to version control.
# ──────────────────────────────────────────────────────────────────────

# ── Supabase Core ────────────────────────────────────────────────────
SUPABASE_URL=https://lzyixazjquouicfsfzzu.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ── Apache Fineract Core Banking ────────────────────────────────────
# Fineract instance URL (without trailing /fineract-provider/api/v1)
FINERACT_BASE_URL=https://your-fineract-instance.com
FINERACT_USERNAME=mifos
FINERACT_PASSWORD=your_password_here
FINERACT_TENANT_ID=default

# ── Fineract Webhook ───────────────────────────────────────────────
# Shared secret for Fineract webhook authentication
# Generate with: openssl rand -hex 32
FINERACT_WEBHOOK_SECRET=your_webhook_secret_here

# ── Loandisk (Legacy — kept for backward compatibility) ────────────
LOANDISK_WEBHOOK_SECRET=your_webhook_secret_here
LOANDISK_API_KEY=your_loandisk_api_key_here
LOANDISK_API_BASE_URL=https://api.loandisk.com

# ── Supabase MCP (used by .mcp.json) ────────────────────────────────
# Same as SUPABASE_SERVICE_ROLE_KEY above; referenced in .mcp.json
# for Claude Code MCP server access.
